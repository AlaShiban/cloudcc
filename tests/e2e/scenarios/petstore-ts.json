{
  "description": "TypeScript, compiled to a container behind a load balancer. The two things it covers that nothing else does: a program written in TypeScript run and deployed rather than only compiled, and a Node container unit -- which until now had no example at all and was verified once, by hand. It also reaches its own store through a tsconfig `paths` alias, so the alias resolution is exercised end to end rather than only in a unit test.",
  "unit": "web",
  "tables": ["pets"],
  "requests": [
    {"method": "GET", "path": "/health"},
    {"method": "GET", "path": "/pets"},
    {"method": "GET", "path": "/pets/1"},
    {"method": "PUT", "path": "/pets/1", "body": {"name": "rex", "species": "dog", "age": 3}},
    {"method": "GET", "path": "/pets/1"},
    {"method": "PUT", "path": "/pets/2", "body": {"name": "mia", "species": "cat", "age": 2}},
    {"method": "GET", "path": "/pets"},
    {"method": "DELETE", "path": "/pets/1"},
    {"method": "GET", "path": "/pets/1"},
    {"method": "GET", "path": "/pets"}
  ]
}
