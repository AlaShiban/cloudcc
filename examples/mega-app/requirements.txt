# Every library mega-app declares something with, grouped the way the example
# is. Unpinned: this tree is a specification, not something CI installs.

# --- web frameworks -------------------------------------------------------
fastapi
flask
django
uvicorn

# --- ORMs -----------------------------------------------------------------
sqlalchemy
sqlmodel
peewee
tortoise-orm

# --- database drivers -----------------------------------------------------
psycopg[binary]
psycopg-pool
pymysql
mysqlclient
asyncpg

# --- other stores ---------------------------------------------------------
redis
pymongo
boto3

# --- messaging ------------------------------------------------------------
celery
pika
aiokafka
kombu

# --- task queues ----------------------------------------------------------
rq
dramatiq

# --- serialization --------------------------------------------------------
pydantic
marshmallow
msgspec

# --- configuration --------------------------------------------------------
python-dotenv
pydantic-settings
dynaconf

# --- logging --------------------------------------------------------------
structlog
loguru

# --- CLI ------------------------------------------------------------------
click
typer
# argparse is in the standard library.

# --- the compiler's own SDK ----------------------------------------------
# Compile-time only: it is stripped from every generated bundle, and it never
# imports boto3.
cloudcompiler
