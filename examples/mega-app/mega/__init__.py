"""Shared modules for mega-app.

One module per library category, so that each mapping is written down exactly
once and the units below stay readable. Nothing here is a unit: importing a
module is what pulls its declarations into a unit's closure.
"""
